import { CarryBalancesModalComponent } from './carry-balances-modal.component';

describe('CarryBalancesModalComponent', () => {
  it('should create', () => {
    const component = Object.create(CarryBalancesModalComponent.prototype) as CarryBalancesModalComponent;
    expect(component).toBeTruthy();
  });
});
